# Secrets — never commit
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Build output
dist/
.next/
.vercel/
*.tsbuildinfo

# OS
.DS_Store

# Obsidian — keep notes + config, drop volatile UI state and trash
obsidian vault/.obsidian/workspace.json
obsidian vault/.trash/
