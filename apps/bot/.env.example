# Copy to apps/bot/.env on the always-on Ubuntu box and fill in.
DISCORD_BOT_TOKEN=
# Use the canonical www host directly — the apex 308-redirects to www, and a POST
# shouldn't take a needless redirect hop.
CONSTELLO_INBOUND_URL=https://www.constello.xyz/api/inbound/discord
DISCORD_INBOUND_SECRET=
