// The public X account readings are announced from — the follow-gated @mention,
// posted by hand from the admin console. One source of truth so the handle never
// drifts between the opt-in UI, the admin post view, and the paste-ready knock.
export const X_HANDLE = "03constello";
