// The public X account readings are announced from — the follow-gated @mention
// (see lib/notify/twitter.ts). One source of truth so the handle never drifts
// between the opt-in UI, the admin verify view, and the outbound mention.
export const X_HANDLE = "03constello";
